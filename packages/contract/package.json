{
  "name": "@midnight-private-voting/contract",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run test/voting.unit.test.ts",
    "test:integration": "vitest run test/voting.integration.test.ts",
    "test:watch": "vitest",
    "compile": "compact compile src/private-voting.compact",
    "compile:skipzk": "compact compile --skip-zk src/private-voting.compact"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.7"
  }
}
